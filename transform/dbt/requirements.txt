dbt-core==1.12.0
dbt-athena==1.11.0
# 6.4c: the `dbt-ol` wrapper (openlineage-dbt) -- entrypoint_dbt.sh runs
# `dbt-ol build` instead of `dbt build` so the RunDbt step emits
# OpenLineage events (model + column-level lineage) to the 6.4b collector.
# Installed here so the one file feeds both local runs and the runner image
# (orchestration/runner/Dockerfile pip-installs this same file). Verified
# 2026-09-06: dbt-ol 1.53.0 wraps dbt-core 1.12 cleanly and never fails the
# build on a transport error (emits 0 events, returns dbt's own exit code).
openlineage-dbt==1.53.0
